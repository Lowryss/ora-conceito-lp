---
phase: 02-secoes-de-produto-e-ambiente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [COL-01, COL-02, COL-03, COL-04]

must_haves:
  truths:
    - "Produtos da Coleção preenchem os cards sem espaço vazio excessivo ao redor"
    - "Nenhum produto aparece apagado ou escurecido sobre o fundo #0E0E0E"
    - "Hover em qualquer card da Coleção revela o label dourado com transição suave"
    - "Em mobile (touch), todos os labels da Coleção são visíveis sem necessidade de hover"
    - "Grid da Coleção colapsa para 2 colunas em 700px sem quebrar proporções"
  artifacts:
    - path: "index.html"
      provides: "CSS .pc img com object-fit:cover + padding:14px, sem mix-blend-mode"
      contains: "object-fit: cover"
    - path: "index.html"
      provides: "@media (hover: none) com .pc-label opacity:1"
      contains: "hover: none"
  key_links:
    - from: ".pc img (CSS linha ~243)"
      to: "cards HTML #colecao"
      via: "object-fit: cover substitui contain"
      pattern: "object-fit: cover"
    - from: ".pc-label (CSS linha ~254)"
      to: "@media (hover: none)"
      via: "opacity:1 fixo em touch"
      pattern: "hover: none"
---

<objective>
Corrigir o CSS da seção Coleção (#colecao) para que produtos preencham os cards corretamente e os labels de hover funcionem em touch.

Purpose: O `object-fit: contain` com `padding: 36px` deixa os produtos pequenos e flutuando. O `mix-blend-mode: multiply` sobre fundo #0E0E0E apaga/escurece as imagens. Ambos são bloqueadores visuais centrais que impedem a seção de transmitir sofisticação.
Output: CSS atualizado em index.html — `.pc img`, `.pc.tall img`, `.pc.wide img`, inline style da linha 659 corrigido, e `@media (hover: none)` adicionado para `.pc-label`.
</objective>

<execution_context>
@C:/Users/Amauri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Amauri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secoes-de-produto-e-ambiente/02-CONTEXT.md
@.planning/phases/02-secoes-de-produto-e-ambiente/02-RESEARCH.md

<interfaces>
<!-- Estado atual dos blocos CSS afetados — extraído de index.html -->

/* index.html linha 243–252 — bloco a modificar */
.pc img {
  width: 100%; height: 100%;
  object-fit: contain;
  padding: 36px;
  aspect-ratio: 1;
  mix-blend-mode: multiply;
  transition: transform .7s ease;
}
.pc.tall img { aspect-ratio: 3/4; padding: 40px 28px; }
.pc.wide img { aspect-ratio: 16/9; padding: 32px 48px; }

/* index.html linha 254–261 — .pc-label (manter estrutura, só adicionar media query) */
.pc-label {
  position: absolute; bottom: 0; left: 0; right: 0;
  padding: 16px 22px;
  background: linear-gradient(to top, rgba(10,10,10,.9) 0%, transparent 100%);
  opacity: 0; transition: opacity .4s;
  display: flex; align-items: flex-end; justify-content: space-between;
}
.pc:hover .pc-label { opacity: 1; }

/* index.html linha 659 — inline style a corrigir */
<img src="assets/foto-10.png" alt="Mesa de Centro" style="aspect-ratio:16/9;padding:40px 80px"/>
/* NOTA: card pai é .pc s6 (sem classe .wide) — aspect-ratio 16/9 vem APENAS do inline style */
/* Solução: adicionar classe wide ao div pai (linha 658) E remover inline style do img */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar CSS object-fit e remover mix-blend-mode da Coleção</name>
  <files>index.html</files>
  <action>
Localizar e modificar os seguintes blocos CSS em index.html (todos dentro da tag `<style>`):

**Bloco 1 — `.pc img` (linha ~243):**
Substituir:
```css
.pc img {
  width: 100%; height: 100%;
  object-fit: contain;
  padding: 36px;
  aspect-ratio: 1;
  mix-blend-mode: multiply;
  transition: transform .7s ease;
}
```
Por:
```css
.pc img {
  width: 100%; height: 100%;
  object-fit: cover;
  padding: 14px;
  aspect-ratio: 1;
  transition: transform .7s ease;
}
```
(Remover `mix-blend-mode: multiply` completamente — não substituir por outro valor.)

**Bloco 2 — `.pc.tall img` e `.pc.wide img` (linha ~251):**
Substituir:
```css
.pc.tall img { aspect-ratio: 3/4; padding: 40px 28px; }
.pc.wide img { aspect-ratio: 16/9; padding: 32px 48px; }
```
Por:
```css
.pc.tall img { aspect-ratio: 3/4; padding: 14px; }
.pc.wide img { aspect-ratio: 16/9; padding: 14px; }
```

**Bloco 3 — Inline style no img da Mesa de Centro (linha ~659 HTML):**

O `<div class="pc s6 reveal">` na linha 658 não tem a classe `wide`. O `aspect-ratio: 16/9` vem apenas do inline style do img filho. Para migrar corretamente:

1. Adicionar classe `wide` ao div: trocar `<div class="pc s6 reveal">` por `<div class="pc s6 wide reveal">`
2. Remover o inline style do img filho: trocar `<img src="assets/foto-10.png" alt="Mesa de Centro" style="aspect-ratio:16/9;padding:40px 80px"/>` por `<img src="assets/foto-10.png" alt="Mesa de Centro"/>`

Dessa forma `.pc.wide img` gerencia tanto o aspect-ratio quanto o padding para esse card.

**Escopo restrito:** NÃO alterar `.gm-item img` (linha ~388, seção Galeria) — esse mix-blend-mode fica para fase posterior.
  </action>
  <verify>
    <automated>MISSING — projeto sem test runner. Verificação via browser após salvar o arquivo.</automated>
    <manual>Abrir index.html no Chrome. Scroll até #colecao. Verificar: (1) produtos preenchem os cards com pequeno respiro nas bordas; (2) nenhum produto com tom escurecido/apagado; (3) DevTools Elements → .pc img → Computed → confirmar `object-fit: cover` e ausência de `mix-blend-mode`; (4) card Mesa de Centro (linha 658–660) mostra proporção 16:9 corretamente.</manual>
  </verify>
  <done>`.pc img` tem `object-fit: cover; padding: 14px` sem `mix-blend-mode`. `.pc.tall img` e `.pc.wide img` têm `padding: 14px`. Div da Mesa de Centro tem classe `wide` e img sem inline style.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar @media (hover: none) para labels touch da Coleção</name>
  <files>index.html</files>
  <action>
Adicionar bloco CSS de media query imediatamente após o bloco `@media(max-width:700px)` existente (linha ~519, antes do fechamento `</style>`).

O bloco a inserir:
```css
/* Touch — labels da Coleção sempre visíveis */
@media (hover: none) {
  .pc-label { opacity: 1; }
}
```

Este bloco usa `@media (hover: none)` — detecta dispositivos touch sem capacidade de hover real (iOS Safari, Android Chrome). É o mesmo padrão já em uso no JS do parallax (`isTouch = window.matchMedia('(hover: none)')`).

NÃO adicionar JS para `.pc-label` — a abordagem CSS-only é declarativa e não depende de timing de execução. O JS existente do parallax (linha ~937) não precisa ser modificado.

NÃO alterar `.dest-overlay` neste plano — o overlay de Ambientes já é sempre visível (sem opacity:0) e é tratado no Plano 02.
  </action>
  <verify>
    <automated>MISSING — projeto sem test runner.</automated>
    <manual>DevTools device toolbar → iPhone SE (375px) → scroll até #colecao → confirmar que todos os labels dourados das peças estão visíveis sem interação. Em desktop normal (1440px, mouse) → labels devem estar ocultos até hover (comportamento original preservado).</manual>
  </verify>
  <done>Bloco `@media (hover: none) { .pc-label { opacity: 1; } }` presente no CSS. Em simulação mobile no DevTools, labels da Coleção visíveis sem hover. Em desktop, labels visíveis somente no hover.</done>
</task>

</tasks>

<verification>
Checklist após execução completa deste plano:

**Desktop (1440px):**
- [ ] Scroll até #colecao — produtos preenchem os cards, fundo escuro aparece apenas como respiro sutil (14px) nas bordas
- [ ] Nenhum produto com aparência escurecida ou apagada (mix-blend-mode removido)
- [ ] Hover em 3+ cards — label dourado aparece com transição suave (opacity 0→1)
- [ ] Card da Mesa de Centro (row 2, span 6) exibe proporção 16:9

**DevTools Elements:**
- [ ] `.pc img` → Computed → `object-fit: cover`, sem `mix-blend-mode`
- [ ] `.pc.tall img` e `.pc.wide img` → `padding: 14px`
- [ ] Linha 658: `<div class="pc s6 wide reveal">`
- [ ] Linha 659: `<img src="assets/foto-10.png" alt="Mesa de Centro"/>` sem style inline

**Mobile — DevTools Device Toolbar (375px iPhone SE):**
- [ ] Labels da Coleção visíveis sem hover
- [ ] Grid colapsa para 2 colunas (`.pc.s3 → span 2`) sem quebra visual
- [ ] Proporções dos cards mantidas (sem imagens distorcidas)

**Regressão:**
- [ ] Hero parallax ainda funciona ao scrollar (JS não alterado)
- [ ] Animações reveal ainda animam ao entrar na viewport
</verification>

<success_criteria>
1. `object-fit: cover; padding: 14px` em `.pc img`, `.pc.tall img`, `.pc.wide img`
2. `mix-blend-mode: multiply` ausente de todos os seletores de `.pc img`
3. `@media (hover: none) { .pc-label { opacity: 1; } }` presente no CSS
4. Card da Mesa de Centro usa classe `wide` sem inline style no img
5. Comportamento visual: produtos proeminentes nos cards, labels touch sempre visíveis
</success_criteria>

<output>
After completion, create `.planning/phases/02-secoes-de-produto-e-ambiente/02-01-SUMMARY.md`
</output>
