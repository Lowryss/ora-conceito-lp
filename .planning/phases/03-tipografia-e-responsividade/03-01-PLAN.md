---
phase: 03-tipografia-e-responsividade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - TIP-01
  - TIP-02
  - TIP-03

must_haves:
  truths:
    - "Body text em todas as seções principais tem tamanho visivelmente uniforme (13px desktop)"
    - "Line-height de 1.9 é perceptivelmente consistente — nenhuma seção parece mais comprimida ou arejada"
    - "Seções têm respiração visual adequada — nenhuma parece colada na seguinte no scroll desktop"
  artifacts:
    - path: "index.html"
      provides: "CSS atualizado com body text 13px e line-height 1.9 em .sobre-txt > p, .dif p, .pilar-d, .contato-txt, .footer-col a"
      contains: "font-size: 13px"
  key_links:
    - from: ".sobre-txt > p"
      to: "13px / line-height 1.9"
      via: "CSS rule update"
      pattern: "sobre-txt.*font-size.*13px"
    - from: ".pilar-d"
      to: "13px / line-height 1.9"
      via: "CSS rule update (era 11.5px)"
      pattern: "pilar-d.*font-size.*13px"
---

<objective>
Padronizar a hierarquia tipográfica do body text em todas as seções da landing page.

Purpose: Unificar os tamanhos e line-heights do corpo de texto que atualmente variam entre 11.5px e 13.5px, e line-heights entre 1.85 e 2.1. O resultado é uma experiência visual coerente — o leitor percebe consistência sem saber por quê.

Output: index.html com CSS de tipografia atualizado. Zero alterações de HTML ou JS.
</objective>

<execution_context>
@C:/Users/Amauri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Amauri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Seletores CSS existentes no index.html que serão modificados. Extraídos diretamente do arquivo. -->

Valores ATUAIS (linha de referência):
```css
/* linha 187-190 */
.sobre-txt > p {
  font-size: 13.5px; line-height: 2.1; color: rgba(242,239,233,.62);
  margin-bottom: 16px;
}

/* linha 206 */
.pilar-d { font-size: 11.5px; line-height: 1.85; color: rgba(242,239,233,.45); }

/* linha 340 */
.dif p { font-size: 12.5px; line-height: 1.9; color: rgba(242,239,233,.52); }

/* linha 414 */
.contato-txt { font-size: 13px; line-height: 2.1; color: rgba(242,239,233,.58); margin-bottom: 40px; }

/* linha 457 */
.footer-col a { font-size: 12.5px; color: rgba(242,239,233,.44); text-decoration: none; transition: color .3s; }
```

Valores TARGET após este plano:
- .sobre-txt > p   → font-size: 13px; line-height: 1.9
- .pilar-d         → font-size: 13px; line-height: 1.9
- .dif p           → font-size: 13px; (line-height 1.9 já está)
- .contato-txt     → line-height: 1.9 (font-size 13px já está)
- .footer-col a    → font-size: 13px
- letter-spacing   → NÃO ALTERAR (variações são intencionais por decisão do usuário)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Padronizar body text — font-size 13px e line-height 1.9</name>
  <files>index.html</files>
  <action>
Editar os seguintes seletores CSS dentro do bloco `&lt;style&gt;` do index.html:

1. `.sobre-txt > p` (linha ~188): alterar `font-size: 13.5px` → `font-size: 13px` e `line-height: 2.1` → `line-height: 1.9`

2. `.pilar-d` (linha ~206): alterar `font-size: 11.5px` → `font-size: 13px` e `line-height: 1.85` → `line-height: 1.9`

3. `.dif p` (linha ~340): alterar `font-size: 12.5px` → `font-size: 13px` (line-height 1.9 já está — não alterar)

4. `.contato-txt` (linha ~414): alterar `line-height: 2.1` → `line-height: 1.9` (font-size 13px já está — não alterar)

5. `.footer-col a` (linha ~457): alterar `font-size: 12.5px` → `font-size: 13px`

NÃO alterar: color, letter-spacing, margin, padding, ou qualquer outro valor além dos especificados acima.
NÃO alterar: headings (.section-title, .hero-h1, .pilar-t, .dif h3), que têm tamanhos intencionalmente maiores.
  </action>
  <verify>
Abrir index.html no browser. DevTools → Elements → selecionar um parágrafo de .sobre-txt > p → aba Computed → confirmar font-size = 13px e line-height = ~24.7px (13 × 1.9). Repetir para .pilar-d, verificando que subiu de 11.5px para 13px. Scroll visual da página em 1440px: hierarquia heading > body text visível em todas as seções.
  </verify>
  <done>
Os cinco seletores têm os valores atualizados. DevTools Computed confirma: .sobre-txt > p = 13px / 1.9, .pilar-d = 13px / 1.9, .dif p = 13px, .contato-txt line-height = 1.9, .footer-col a = 13px. Nenhum outro valor CSS foi alterado.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Inspecionar e ajustar respiração visual das seções (TIP-03)</name>
  <files>index.html</files>
  <action>
Abrir index.html no browser em 1440px e fazer scroll completo da página. Identificar seções onde o padding/margin pareça visivelmente inadequado (muito apertado ou desproporcional ao restante).

Referências de padding existentes:
- `#sobre`: padding via grid (implícito pelo .sobre-txt: 100px 80px)
- `#numeros`: verificar visualmente
- `#colecao` / `#destaques` / `#diferenciais` / `#galeria`: verificar visualmente
- `#contato`: padding:80px 32px em 1200px — verificar se desktop tem valor adequado

Se alguma seção parecer visivelmente apertada comparada às outras, adicionar ou ajustar `padding` nessa seção específica. Se tudo parecer adequado, não fazer alteração nenhuma (este é um ajuste de discretion — só modificar se houver necessidade clara).

NÃO alterar seções que já pareçam bem espaçadas. NÃO adicionat padding genérico "por precaução".
  </action>
  <verify>
Scroll completo da página em 1440px, 1024px e 375px. Nenhuma seção deve parecer colada na seguinte ou visivelmente mais apertada que as vizinhas.
  </verify>
  <done>
Inspeção visual completa realizada. Ajustes pontuais feitos apenas onde necessário (pode ser zero alterações se tudo estiver adequado). Seções têm respiração visual consistente em todos os breakpoints.
  </done>
</task>

</tasks>

<verification>
Verificação manual completa após execução:

**TIP-01** — DevTools Computed em viewport 1440px:
- `.sobre-txt > p`: font-size = 13px (era 13.5px)
- `.pilar-d`: font-size = 13px (era 11.5px)
- `.dif p`: font-size = 13px (era 12.5px)
- Hierarquia visível: section-title claramente maior que body text

**TIP-02** — DevTools Computed:
- `.sobre-txt > p`: line-height = 1.9 (era 2.1)
- `.contato-txt`: line-height = 1.9 (era 2.1)
- `.pilar-d`: line-height = 1.9 (era 1.85)
- Inspeção visual: nenhuma seção parece mais arejada ou comprimida

**TIP-03** — Inspeção visual:
- Scroll desktop 1440px: seções respiram naturalmente
- Scroll 375px: padding lateral suficiente em todas as seções
</verification>

<success_criteria>
- Os 5 seletores de body text têm font-size 13px e/ou line-height 1.9 conforme especificado
- Nenhum valor não especificado foi alterado (colors, letter-spacing, margins intactos)
- Inspeção visual não revela inconsistência de espaçamento entre seções
- Zero regressões visuais em desktop (1440px)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-tipografia-e-responsividade/03-01-SUMMARY.md`
</output>
